import { N50A, N60A } from './colors'

export const shadows = {
    100: `box-shadow: 0 1px 1px ${N50A}, 0 0 1px 0 ${N60A};`,
    200: `box-shadow: 0 4px 8px -2px ${N50A}, 0 0 1px ${N60A};`,
    300: `box-shadow: 0 8px 16px -4px ${N50A}, 0 0 1px ${N60A};`,
    400: `box-shadow: 0 12px 24px -6px ${N50A}, 0 0 1px ${N60A};`,
    500: `box-shadow: 0 20px 32px -8px ${N50A}, 0 0 1px ${N60A};`,
  }
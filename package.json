{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn --cwd packages/server start:database && yarn --cwd packages/server generate & yarn --cwd packages/server start & yarn --cwd packages/client start"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^1.5.0",
    "@typescript-eslint/parser": "^1.5.0",
    "eslint-config-prettier": "^4.1.0",
    "prettier": "^1.16.4",
    "typescript": "^3.4.1"
  }
}
